    // just require these modules without using them so that they get included
    // in the common/react module during the build
define([
    "react",
    "react-dom",
    "lib/handle-ref"
], () => {});
