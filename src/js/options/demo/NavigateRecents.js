import React from "react";
import { DemoRoot } from "@/options/demo/DemoRoot";

export default function NavigateRecents({
	width = 250,
	height })
{
	return (
		<DemoRoot
			width={width}
			height={height}
		/>
	);
}
